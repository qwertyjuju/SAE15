Pour que le programme puisse créer le fichier excel, il faut installer openpyxl. Vous pouvez lancer le fichier "install.bat" ou faire la commande: "pip install openpyxl" dans un terminal

Vous devez mettre le fichier a traiter dans le repertoire du programme

Pour lancer le programme, appuyer sur Analyser.py

